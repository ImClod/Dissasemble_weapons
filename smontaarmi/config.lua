Config = {}
Config.KRZ = false --####### set on true if using krz_personalmenu, false if  using menu_default

Config.UseKeyBind = true ---set True if u want to use keybind
Config.KeyBind = 'O' -- WILL WORK ONLY IF CONFIG.USEKEYBIND == TRUE this is the default keybind it can be changed in fivem settings > keybinds > fivem > Tasto per aprire il menu smontaarmi (smontaarmi)
Config.Weapons ={
    WEAPON_PISTOL = {
        item = 'pistola'
    },
    WEAPON_PISTOL50 = {
        item = 'WEAPON_PISTOL50'
        
    }
}